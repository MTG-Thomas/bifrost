# Docs-only PR notice. These jobs intentionally do not share required CI
# status-check names; real CI runs for every pull request.

name: Docs-only CI Notice

on:
  pull_request:
    branches: [main]
    paths:
      - "**/*.md"
      - "**/*.txt"
      - "**/*.png"
      - "**/*.jpg"
      - "**/*.jpeg"
      - "**/*.gif"
      - "**/*.svg"
      - "**/*.webp"
      - "**/*.ico"
      - ".agents/**"
      - ".claude/**"
      - ".claude-plugin/**"
      - ".codex/**"
      - ".codex-plugin/**"
      - "plugins/bifrost/**"
      - "docs/**"
      - "LICENSE"
      - ".github/CODEOWNERS"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/dependabot.yml"
      - ".gitignore"
      - ".gitattributes"
      - ".editorconfig"
      - ".prettierrc*"
      - ".prettierignore"
      - ".vscode/**"
      - ".idea/**"

permissions:
  contents: read

jobs:
  docs-only-notice:
    runs-on: ubuntu-latest
    name: Docs-only CI notice
    steps:
      - run: echo "Docs-only change — real CI runs from ci.yml when code changes."
