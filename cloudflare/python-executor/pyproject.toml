[project]
name = "bifrost-cloudflare-python-executor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "humanize>=4.12,<5",
]

[dependency-groups]
dev = [
    "workers-py>=1.9.0",
    "workers-runtime-sdk",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
