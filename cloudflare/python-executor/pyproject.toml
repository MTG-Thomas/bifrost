[project]
name = "bifrost-cloudflare-python-executor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "httpx",
    "pydantic",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
